{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "migrate": {
      "outputs": []
    },
    "generate": {
      "dependsOn": ["^generate"]
    }
  }
}
