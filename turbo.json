{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "env": ["DATABASE_URL"],
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "migrate": {
      "env": ["DATABASE_URL"],
      "outputs": []
    },
    "generate": {
      "env": ["DATABASE_URL"],
      "dependsOn": ["^generate"],
      "cache": false
    },
    "studio": {
      "env": ["DATABASE_URL"],
      "cache": false
    }
  }
}
